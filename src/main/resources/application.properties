# ==========================================
# 1. SERVER CONFIG
# ==========================================
server.port=${PORT:8081}

# ==========================================
# 2. DATABASE CONFIG (Aiven Cloud)
# ==========================================
spring.datasource.url=jdbc:mysql://mysql-3a3c10a-lucy139200556-8820.k.aivencloud.com:21387/defaultdb?useSSL=true&serverTimezone=UTC
spring.datasource.username=avnadmin
# ⚠️ Lưu ý: Nếu chạy ở máy tính, hãy thay chữ ${AIVEN_DB_PASSWORD} bằng mật khẩu thật. 
# Khi đưa lên Render/GitHub thì trả lại chữ ${AIVEN_DB_PASSWORD}
spring.datasource.password=AVNS_d2Xp46VuPevf0NgBaew

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# ==========================================
# 3. WEB & FILE CONFIG
# ==========================================
spring.thymeleaf.cache=false
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# ==========================================
# 4. EMAIL CONFIG
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=lucy139200556@gmail.com
# ⚠️ Lưu ý: Tương tự mật khẩu DB, chạy máy tính thì dán pass mail vào, lên web thì dùng biến
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==========================================
# 5. VNPAY CONFIG
# ==========================================
vnpay.tmnCode=N77AE6DX
vnpay.hashSecret=DF7DRKZA51AL9BKT3GWM8XLQB4ZIU89R
vnpay.payUrl=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.apiUrl=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction

# ⚠️ CHỌN 1 TRONG 2 DÒNG DƯỚI ĐÂY (Dòng nào không dùng thì thêm dấu # ở đầu)

# Dùng khi code đang chạy test ở máy tính:
vnpay.returnUrl=http://localhost:8081/payment/vnpay-return

# Dùng khi web đã đưa lên Render chạy thật (Nhớ thay link web Render của bạn vào):
# vnpay.returnUrl=https://web-perfume-doancuoikhoa-XXXX.onrender.com/payment/vnpay-return